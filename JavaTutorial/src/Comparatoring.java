import java.util.Comparator;

//comparator
//compares existing objects instead of 2 created objects like compareto

public class Comparatoring {
	public static class PointComparator implements Comparator<int[]> {
		public int compare(int []a, int []b) {
			double da = Math.sqrt(a[0] * a[0] + a[1] * a[1]);	
		}
	}
	public static void main (String)
}
//anonymous functions